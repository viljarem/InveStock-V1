name: 📈 Daglig Dataoppdatering (Streamlit Cloud)

on:
  schedule:
    # Kjører hver dag kl 17:00 norsk tid
    - cron: '0 16 * * 1-5'  # Mandag-fredag kl 17:00 CET
  
  workflow_dispatch:  # Manuell kjøring

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Hent kode
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.PERSONAL_ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - name: 🐍 Sett opp Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: 📦 Installer pakker
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 📈 Oppdater markedsdata
      run: python update_market_data.py
    
    - name: 💾 Push til Streamlit Cloud
      run: |
        # Sjekk om det er endringer
        if [ -z "$(git status --porcelain)" ]; then
          echo "✅ Ingen nye data å pushe"
          exit 0
        fi
        
        # Konfigurer Git
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        
        # Add og commit kun datafiler
        git add data_storage/*.parquet data_storage/*.json
        git commit -m "🤖 Automatisk dataoppdatering $(TZ='Europe/Oslo' date '+%H:%M %d.%m.%Y')"
        
        # Push til Streamlit Cloud
        git push
        echo "🚀 Fersk data sendt til Streamlit Cloud!"